---
// The /en homepage — the canonical "core" landing. Content lives in the shared
// Home component so the bare site root (src/pages/index.astro) can render the
// exact same page. Canonical resolves to this URL (/en) by default.
import Home from '~/components/Home.astro';
---

<Home locale="en" />
